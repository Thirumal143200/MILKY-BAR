name: Mobile CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    name: Mobile Build & Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Expo
        if: secrets.EXPO_TOKEN != ''
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: npm run build --workspace=packages/shared

      - name: Type Check & Lint
        run: |
          npm run type-check --workspace=mobile
          npx eslint mobile/src --ext .ts,.tsx -c .eslintrc.cjs

      - name: EAS Build Status Notice
        if: github.ref == 'refs/heads/main' && secrets.EXPO_TOKEN == ''
        run: echo "EXPO_TOKEN secret is not set in repository secrets; skipping EAS cloud build."

      - name: Build Android (EAS)
        if: github.ref == 'refs/heads/main' && secrets.EXPO_TOKEN != ''
        run: |
          cd mobile
          eas build --platform android --profile production --non-interactive
